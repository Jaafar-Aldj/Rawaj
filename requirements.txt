# --- AI Core (This will install compatible google/grpc libs automatically) ---
# ag2[gemini,retrievechat]>=0.10.3
# google-generativeai>=0.8.3

# --- Essential Fixes ---
# Protobuf must be pinned to avoid conflicts with older Google libs
# protobuf>=4.25.0,<5.0.0

# --- Backend ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart
pyjwt

# --- Database ---
sqlalchemy>=2.0.0
# Choose one of these, usually you don't need both binary and pure
psycopg2-binary 
alembic

# --- Utilities ---
python-dotenv
pydantic>=2.0.0
requests
moviepy>=1.0.3
pwdlib[argon2]
fastapi-mail
# Rembg can be heavy, usually best to install last if it causes issues
rembg 
# onnxruntime
pillow
gTTS